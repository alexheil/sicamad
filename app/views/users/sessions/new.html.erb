<div class="form-background">
  <header>
  <nav>
    <div class="logo">
      <%= link_to "[−]", root_url %>
    </div>
  </nav>
  <div class="category-title">
    [sign in]
  </div>
</header>

<div class="wrapper">
    <div class="container">

      <div class="form-slate">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

          <div class="field">
            <%= f.email_field :email, autofocus: true, placeholder: "Email address", class: "text-field" %>
          </div>
          <div class="field">
            <%= f.password_field :password, autocomplete: "off", placeholder: "Password", class: "text-field" %>
          </div>

          <% if devise_mapping.rememberable? -%>
            <div class="field">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
          <% end -%>

          <div class="actions">
            <%= f.submit "Sign in", class: "submit-button" %>
          </div>
        <% end %>
      </div>

    </div>
  </div>
</div>
